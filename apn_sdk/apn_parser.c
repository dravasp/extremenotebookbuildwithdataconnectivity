#include <stdio.h>
int parse_apn_xml(const char* filename) { printf("Parsing XML: %s\n", filename); return 0; }
int parse_apn_mobileconfig(const char* filename) { printf("Parsing mobileconfig: %s\n", filename); return 0; }
